services:
  poetry-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - CHROMA_PERSIST_DIR=/app/data/chroma_db
      - CORPUS_DIR=/app/data/corpus
    restart: unless-stopped
